{
    "get_real_estate_info_by_name": {
        "name": "get_real_estate_info_by_name",
        "description": "Get the information of a real state company by name stored in the database",
        "parameters": {
            "type": "object",
            "properties":  {
                "name": {
                    "type": "string",
                    "description": "the name of the Gen AI Tool"
                }
            },
            "required": ["name"]
        }
    },
    "get_real_estate_listings": {
        "name": "get_real_estate_listings",
        "description": "Get the list of real-state properties from the SQLite database",
        "parameters": {
            "type": "object",
            "properties": {},
            "required": []
        }
    },
    "insert_real_estate_info": {
        "name": "insert_real_estate_info",
        "description": "Insert a single record in the SQLite database",
        "parameters": {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "Name of the real state company or listing"
                },
                "link": {
                    "type": "string",
                    "description": "URL of the official website of the real state company or listing"
                },
                "category": {
                    "type": "string",
                    "description": "Indicate wether is a company or a real state listing"
                },
                "description": {
                    "type": "string",
                    "description": "A short summary that describes the real state listing or company, not more than 500 words"
                }
            },
            "required": [
                "name",
                "link",
                "category",
                "description"
            ]
        }
    },
    "update_real_estate_info": {
        "name": "update_real_estate_info",
        "description": "Insert a single record in the SQLite database",
        "parameters": {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "Name of the real state company or listing"
                },
                "link": {
                    "type": "string",
                    "description": "URL of the official website of the real state company or listing"
                },
                "category": {
                    "type": "string",
                    "description": "Indicate wether is a company or a real state listing"
                },
                "description": {
                    "type": "string",
                    "description": "A short summary that describes the real state listing or company, not more than 500 words"
                }
            },
            "required": [
                "name",
                "link",
                "category",
                "description"
            ]
        }
    }
}